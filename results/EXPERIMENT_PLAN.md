# 实验计划

## 实验 0: 指令能不能覆盖 canvas 位置
用 example3 的猴+女孩, canvas 固定猴左女右。
两条指令只交换左右, 各 5 seed。
输出左右跟指令走 = 可控; 输出永远猴左女右 = 位置由 canvas 决定, 指令无效。
成本 ¥1.5, 零数据准备。

## 实验 1: 两张相似 ref 会不会搞反
错配率 = 错的张数 / N, 报 95% CI。
错的定义: 搞反 / 混合 / 少一个 / 都不像。只看事先声明的那一个区分属性。
N=50。对照: 单 ref 和极不相似的两张, 错配率都该 ≈ 0, 否则是我判定方法有问题。

**阈值(看数字前不许改):**
- CI 下界 > 30% → 做方法
- 与 20% 重叠 → 扩到 100 再看
- CI 上界 < 20% → 换问题

## 实验 2: 加 mask 能不能修好 (实验1 强信号才做)
同一批样本, baseline vs +mask 配对比, McNemar。
必须同时跑两个对照: 只绑文本 token; 单 ref 跑 K 次。
任一能修好, 我的方法就没必要。

## 纪律
再抽象一次为限, 且必须由数据触发, 不由文献搜索触发。